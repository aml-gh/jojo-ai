import express from 'express';import dotenv from 'dotenv';import OpenAI from 'openai';dotenv.config();const app=express();app.use(express.json());app.use(express.static('.'));const ai=new OpenAI({apiKey:process.env.OPENAI_API_KEY});app.post('/api/chat',async(req,res)=>{try{const r=await ai.responses.create({model:'gpt-4o-mini',input:'أجب بالعربية كمساعدة اسمها جوجو: '+req.body.message});res.json({reply:r.output_text})}catch(e){res.json({reply:'حدث خطأ'})}});app.listen(3000);